Copy the UnityScripts folder into the Assets folder of the Unity project and apply the contents to the Avatar object in the scene and then allocate the required prefabs to their required positions.

Place the WebPageScripts folder into the folder that you will instruct Unity to build the WebApp into.

Replace the WebPlayer.html generated by Unity with the contents of the WebPlayer folder.